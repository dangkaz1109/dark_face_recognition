model:
  backbone_pretrained: 'vggface2'
  embedding_dim: 512

training:
  epochs: 50
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0001
  seed: 42
  train_with_noise: true

data:
  csv_path: 'data/Dataset.csv'
  img_dir: 'data/Faces/Faces'
  img_size: 160
  max_pairs_per_class: 50
